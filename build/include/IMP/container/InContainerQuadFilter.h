// Autogenerated by tools/build/make_containers.py
// from tools/build/container_templates/container/InContainerClassnameFilter.h
// Do not edit - any changes will be lost!

/**
 *  \file IMP/container/InContainerQuadFilter.h
 *  \brief A filter for Quads.
 *
 *  Copyright 2007-2022 IMP Inventors. All rights reserved.
 */

#ifndef IMPCONTAINER_IN_CONTAINER_QUAD_FILTER_H
#define IMPCONTAINER_IN_CONTAINER_QUAD_FILTER_H

#include <IMP/container/container_config.h>
#include <IMP/QuadPredicate.h>
#include <IMP/QuadContainer.h>
#include <IMP/internal/container_helpers.h>
#include "internal/QuadContainerIndex.h"
#include <IMP/quad_macros.h>
#include <IMP/object_macros.h>
#include <IMP/warning_macros.h>

IMPCONTAINER_BEGIN_NAMESPACE

//! A filter which returns true if a container contains the Quad
/** This predicate returns 1 if the passed tuple is in the container.
    \note Use the handle_permutations parameter to the constructor to
    determine whether only exact matches, or matches under permutation
    are considered matching. By default permutations are allowed.
 */
class IMPCONTAINEREXPORT InContainerQuadFilter
    : public QuadPredicate {
  IMP::PointerMember<internal::QuadContainerIndex> c_;

 public:
  InContainerQuadFilter(QuadContainer *c,
                             std::string name = "QuadFilter %1%");
  InContainerQuadFilter(QuadContainer *c, bool handle_permutations,
                             std::string name = "QuadFilter %1%");

  virtual int get_value_index(Model *, const ParticleIndexQuad& vt) const
      override {
    return c_->get_contains(vt);
  }
  virtual ModelObjectsTemp do_get_inputs(
      Model *m, const ParticleIndexes &pi) const override {
    ModelObjectsTemp ret = IMP::get_particles(m, pi);
    ret.push_back(c_);
    return ret;
  }
  IMP_QUAD_PREDICATE_METHODS(InContainerQuadFilter);
  IMP_OBJECT_METHODS(InContainerQuadFilter);
};

IMP_OBJECTS(InContainerQuadFilter, InContainerQuadFilters);

IMPCONTAINER_END_NAMESPACE

#endif /* IMPCONTAINER_IN_CONTAINER_QUAD_FILTER_H */
