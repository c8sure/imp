// Autogenerated by tools/build/make_containers.py
// from tools/build/container_templates/kernel/ClassnameContainer.h
// Do not edit - any changes will be lost!

/**
 *  \file IMP/SingletonContainer.h
 *  \brief A container for Singletons.
 *
 *  Copyright 2007-2022 IMP Inventors. All rights reserved.
 */

#ifndef IMPKERNEL_SINGLETON_CONTAINER_H
#define IMPKERNEL_SINGLETON_CONTAINER_H

#include <IMP/kernel_config.h>
#include "internal/IndexingIterator.h"
#include "Particle.h"
#include "container_base.h"
#include "internal/container_helpers.h"
#include "DerivativeAccumulator.h"
#include "base_types.h"
#include <IMP/ref_counted_macros.h>
#include <IMP/check_macros.h>
#include <IMP/Pointer.h>
#include <IMP/InputAdaptor.h>
#include <IMP/utility_macros.h>
#include <IMP/deprecation_macros.h>
#include <algorithm>
#include <cereal/access.hpp>
#include <cereal/types/base_class.hpp>

IMPKERNEL_BEGIN_NAMESPACE
class SingletonModifier;
class SingletonScore;

//! A shared container for Singletons
/** Stores a shared collection of Singletons.
 */
class IMPKERNELEXPORT SingletonContainer : public Container {
 public:
  typedef Particle* ContainedType;
  typedef ParticlesTemp ContainedTypes;
  typedef ParticleIndexes ContainedIndexTypes;
  typedef ParticleIndex ContainedIndexType;
  typedef SingletonModifier Modifier;
  typedef ParticleIndex PassContainedIndexType;

  //! Just use apply() in the base class
  void apply_generic(const SingletonModifier *m) const;

  //! Just use apply_moved() in the base class
  void apply_generic_moved(const SingletonModifier *m,
                           const ParticleIndexes &moved_pis,
                           const ParticleIndexes &reset_pis) const;

  //! Apply a SingletonModifier to the contents
  void apply(const SingletonModifier *sm) const;

  //! Apply a SingletonModifier to the contents
  void apply_moved(const SingletonModifier *sm,
                   const ParticleIndexes &moved_pis,
                   const ParticleIndexes &reset_pis) const;

  /** Get all the indexes that might possibly be contained in the
      container, useful with dynamic containers. For example,
      with a container::ClosePairContainer, this is the list
      of all pairs taken from the input list (those that are far apart
      in addition to those that are close).
  */
  virtual ParticleIndexes get_range_indexes() const = 0;

  const ParticleIndexes &get_contents() const {
    if (get_provides_access())
      return get_access();
    else {
      std::size_t nhash = get_contents_hash();
      if (contents_hash_ != nhash || !cache_initialized_) {
        contents_hash_ = nhash;
        cache_initialized_ = true;
        get_indexes_in_place(contents_cache_); // inheriting class could implement this faster than get_indexes()
      }
      return contents_cache_;
    }
  }

  /** Get all the indexes contained in the container.

    This should be protected but isn't for compatibility reasons.

    External callers should use get_contents().
  */
  virtual ParticleIndexes get_indexes() const = 0;


#ifndef IMP_DOXYGEN

  ParticlesTemp get() const {
    return IMP::internal::get_particle(get_model(), get_indexes());
  }

  Particle* get(unsigned int i) const {
    return IMP::internal::get_particle(get_model(), get_indexes()[i]);
  }

  //! Return size of current container content
  //! Note that this may be expensive since it may involve
  //! refreshing of the container
  unsigned int get_number() const { return get_indexes().size(); }
#ifndef SWIG
  bool get_provides_access() const;
  virtual const ParticleIndexes &get_access() const {
    IMP_THROW("Object not implemented properly.", IndexException);
  }

  template <class Functor>
  Functor for_each(Functor f) {
    ParticleIndexes vs = get_indexes();
    // use boost range instead
    return std::for_each(vs.begin(), vs.end(), f);
  }

#endif
#endif

 protected:
  SingletonContainer(Model *m,
                     std::string name = "SingletonContainer %1%");
  SingletonContainer() {}

  virtual void do_apply(const SingletonModifier *sm) const = 0;
  virtual void do_apply_moved(const SingletonModifier *sm,
                              const ParticleIndexes &moved_pis,
                              const ParticleIndexes &reset_pis) const {
    IMP_UNUSED(moved_pis);
    IMP_UNUSED(reset_pis);
    do_apply(sm);
  }
  virtual bool do_get_provides_access() const { return false; }

#if !defined(SWIG) && !defined(IMP_DOXYGEN)
  //! Insert the contents of the container into output
  //! Could be reimplemented to save time in terms of e.g. memory
  //! allocation compared with calling get_indexes()
  virtual void get_indexes_in_place(ParticleIndexes& output) const{
    output= get_indexes();
  }
#endif

  IMP_REF_COUNTED_NONTRIVIAL_DESTRUCTOR(SingletonContainer);

 private:
  mutable std::size_t contents_hash_;
  mutable ParticleIndexes contents_cache_;
  mutable bool cache_initialized_;

  friend class cereal::access;

  template<class Archive> void serialize(Archive &ar) {
    ar(cereal::base_class<Container>(this));
    // clear cache on read
    if (std::is_base_of<cereal::detail::InputArchiveBase, Archive>::value) {
      contents_hash_ = -1;
      cache_initialized_ = false;
    }
  }

};

/** This class allows either a list or a container to be
    accepted as input.
*/
class IMPKERNELEXPORT SingletonContainerAdaptor :
#if !defined(SWIG) && !defined(IMP_DOXYGEN)
    public Pointer<SingletonContainer>
#else
    public InputAdaptor
#endif
    {
  typedef Pointer<SingletonContainer> P;

 public:
  SingletonContainerAdaptor() {}

  /**
     Constructs the adaptor pointing to c (so if the contents of c are changed
     dynamically, so do the contents of the adaptor, and vice versa)
   */
  SingletonContainerAdaptor(SingletonContainer *c);

  /**
     Constructs the adaptor pointing to c (so if the contents of c are changed
     dynamically, so do the contents of the adaptor, and vice versa)
   */
  template <class C>
  SingletonContainerAdaptor(IMP::internal::PointerBase<C> c)
      : P(c) {}

  /**
     Adapts the non-empty list t to SingletonContainer

     @param t a non-empty list of ParticlesTemp
  */
  SingletonContainerAdaptor(const ParticlesTemp &t);

  /** Set the name of the resulting container if it is currently the
      default value. */
  void set_name_if_default(std::string name);
};

IMPKERNEL_END_NAMESPACE

#endif /* IMPKERNEL_SINGLETON_CONTAINER_H */
